[workspace]
# Don't publish to crates.io (GitHub releases only)
publish = false

# Enable git tags and GitHub releases
git_tag_enable = true
git_release_enable = true

# Release tag format
git_tag_name = "v{{ version }}"
git_release_name = "v{{ version }}"
